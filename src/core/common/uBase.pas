unit uBase;

interface

const
  Programm = 'IntelligeN';
  ProgrammName = Programm + ' 2009';

type
  TFileSystem = (fsRoot, fsConfig, fsPlugins, fsSettings, fsCMS, fsCMSSubject, fsCMSMessage, fsSite, fsType);

  TViewType = (vtData, vtCode, vtPreview);

implementation

end.

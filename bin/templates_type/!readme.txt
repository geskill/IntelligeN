These files represent the list of types inside intelligen.
Every file is based upon a static category. You can change
the order inside of those files, add or remove controls.

Every control is static. You cannot add a new control!

*.xml will be listed only